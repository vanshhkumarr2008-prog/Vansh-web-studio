## Packages
(none needed, all required packages like next-themes, react-hook-form, zod, framer-motion are already installed)

## Notes
- Single-page scrolling landing page design with sections (Hero, Services, Portfolio, Contact).
- Dark mode implemented using `next-themes`.
- Primary colors configured strictly to match the requested Navy Blue agency aesthetic.
- Forms connected to `/api/contact`.
